"""Agent tools for local-operator.

``builtin.py`` holds the :class:`~local_operator.harness.types.AgentTool`
implementations (pydantic JSON Schemas, executed via native provider tool
calling); ``registry.py`` is the entry point —
``local_operator.tools.registry.create_tools``.
"""
