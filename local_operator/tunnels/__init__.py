"""Radient personal tunnels: cloud ownership, a local authenticated gateway.

Imported only by ``lop tunnel`` / ``/mobile``. A Radient login makes tunnel
creation available; it never publishes a listener without an explicit command.
"""
