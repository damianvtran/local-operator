
== C1: the advisor does not mutate the session's history ==
  [PASS] live history unchanged after a real advisor call: 3 messages, same identities and contents
  [PASS] advisor turn rides on the REQUEST only: wire=4 msgs vs live=3; question is the tail
  [PASS] wire prefix equals the live history: append-only against the turn's own prefix
  [PASS] live tools sent (front of the cache prefix): 5 tools, tool_choice='none'; sending [] would change position 0 and force a full re-process

== C2: the advisor request is APPEND-ONLY on the cache prefix ==
  [PASS] message prefix identical: turn=12 msgs is a strict prefix of advisor=13 msgs (+1 appended)

== C3: cache_control breakpoints are not added or moved ==
  [PASS] breakpoint count within cap: turn=2 advisor=2 cap=4
  [PASS] system breakpoints unmoved: turn=[] advisor=[]
  [PASS] no breakpoint moves LEFT into the shared prefix: turn marks [8, 11], advisor marks [10, 12] -- all at or right of the turn's earliest; the shared prefix ahead of index 8 keeps its cache entries (live proof: arm 3 re-warm = 100% cache read)

== C4: system blocks are passed through unchanged ==
  [PASS] system array identical: 2 blocks, byte-identical
  [PASS] control: system-block placement DOES diverge: the rejected arm 6 shape changes the prefix, so C4 is a live check

== C5: prompt_cache_key survives on the advisor request ==
  [PASS] cache key preserved: turn='session-abc' advisor='session-abc'
  [PASS] isolated=False is what keeps it: shipped isolated=False; isolated=True would strip the key on the OpenAI wire (control object isolated=True)

== C6: off by default is inert and byte-identical ==
  [PASS] default is OFF: advisor_enabled default=False
  [PASS] pre-feature config resolves identically: every non-advisor field matches a config that predates the feature
  [PASS] gate returns None with the flag off: off->None; on->settings (gate is live, not dead code)

======================================================================
RESULT: 15/15 checks passed
