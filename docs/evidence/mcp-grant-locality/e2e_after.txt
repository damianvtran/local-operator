runtime listening on 127.0.0.1:55459

[1] LOCAL client, `/mcp reauth notion`
    reply : authorizing MCP server 'notion' — a browser tab is opening on this machine; the result appears here when it completes.
    style : info
    PASS  : the grant was started
    effect: forgot=['notion'] disconnected=['notion'] connected=['notion']
    PASS  : reauth forgot the old grant, then reconnected
    notice: [("authenticated MCP server 'notion'; 5 tools available.", 'info')]
    PASS  : outcome delivered as a NoticeEvent

[2] REMOTE client (a future phone relay), same command
    reply : /mcp reauth opens a browser and stores credentials on the machine running the session — run it from a terminal on that machine
    effect: disconnected=[] connected=[]
    PASS  : refused, and nothing was touched

[3] `/mcp reauth` -> usage: /mcp reauth <name>
[3] `/mcp reauth a b` -> /mcp reauth takes one server name — got 'a b'
[3] `/mcp bogus x` -> unknown mcp subcommand: bogus — try /mcp list|add|remove|login|logout|reauth <name>
    PASS  : malformed commands ran nothing

RESULT: ALL CHECKS PASSED
