############ F2 / U6 (blocker) — a REAL hung tool is bounded ############
The round-2 regression: a blocking stdout.read() ran BEFORE the deadline
check, so a tool holding stdout open never reached it. 15s X11 / 12s macOS
measured against a 2.0s budget, child orphaned.

load average: 143.54 117.60 94.24
  hung tool, bounded read    returned 2.02s (budget 2.0s) -> None  orphans: none
  hung tool, unbounded read  returned 2.02s (budget 2.0s) -> None  orphans: none
  answers then hangs         returned 2.00s (budget 2.0s) -> None  orphans: none
  healthy tool               -> b'hello'

############ U3 / U7 (major/minor) — latency, measured at the composer ############
Round 2 measured 4-9s to a marker and 4203ms for a whitespace paste. The
cost was osascript SPAWNS: 2-4s of wall time each against ~0.25s of CPU,
and the miss path paid two of them. One script now answers both shapes,
and a copied-whitespace paste does not consult the clipboard at all.

  TEXT paste (control)               24 ms  -> 'hello world'
  WHITESPACE paste (was 4203ms)     106 ms  -> '    '
  TAB paste (was 3850ms)            157 ms  -> '\t'
  EMPTY -> screenshot (was 4-9s)   1825 ms  -> '[Image #1, 1568x1014 ↓] '

############ D9 (minor) — copied whitespace is no longer traded for an image ############
  four spaces    pasted='    '   -> 'def handler():\n    '                       whitespace kept: True
  tab            pasted='\t'     -> 'def handler():\n\t'                         whitespace kept: True
  EMPTY signal   pasted=''       -> 'def handler():\n[Image #1, 1568x200] '      whitespace kept: True

############ D8 (major) — the path route retires a held notice ############
The notice tells the user to paste a file path. They do, it works, and the
card used to come back afterwards to say it did not.
  1. MCP failure holds the slot
  2. Cmd+V over SSH  deferred="Clipboard images aren't read over SSH. Paste a file path instead."
  3. user pastes the FILE PATH, as instructed
       editor  ='[Image #1, 400x100] '
       deferred=None   <- retired
  4. MCP card expires -> showing='' display=False

############ D10 / D11 (minor) — message accuracy and toast rank ############
  empty / text-only clipboard  actionable=False "Couldn't attach an image from the clipboard."
  image found, unattachable    actionable=True  "Couldn't attach that image. It may be too large or corrupt."
  file copied, unreadable      actionable=True  "Couldn't attach that file. Only PNG, JPEG, GIF and WebP images attach."
  over SSH                     actionable=True  "Clipboard images aren't read over SSH. Paste a file path instead."
