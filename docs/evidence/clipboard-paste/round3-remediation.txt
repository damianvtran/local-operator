############ F4 (major) — direct child exits, descendant holds the pipe ############
poll() returns 0, so a kill gated on it was skipped; the reader stayed
blocked and Popen.__exit__ deadlocked the MAIN thread on stdout.close().
Measured before the fix: never returned after 20s on a 2.0s budget.

load average: 43.43 49.10 64.61
  returned 2.00s (budget 2.0s) -> None
  survivors: none

############ D12 (major) — one image, both routes, same answer ############
The clipboard route bounds before the attachment cap (U1's fix); the path
route refused at stat() against that cap, so Finder Cmd+C rejected a
screenshot Cmd+V attached, and blamed its format.
  source PNG: 6.7 MB (attachment cap 4.0 MB)
  Cmd+V (clipboard bytes)    editor='[Image #1, 1568x543 ↓] '  toast=''
  Finder Cmd+C (file URL)    editor='[Image #1, 1568x543 ↓] '  toast=''

############ D12 — the five causes that reach the file notice ############
  A non-image .txt         -> ''                     Couldn't attach that file. It may be too large, or not an image.
  C HEIC                   -> ''                     Couldn't attach that file. It may be too large, or not an image.
  D good PNG + .txt        -> ''                     Couldn't attach that file. It may be too large, or not an image.
  E path does not exist    -> ''                     Couldn't attach that file. It may be too large, or not an image.
  F control: small PNG     -> '[Image #1, 40x20] '   (no notice)

############ D13 (minor) — a SHOWING card is retired by the paste answering it ############
  SHOWING case
    raised   "Clipboard images aren't read over SSH. Paste a file path instead."
    attached editor='[Image #1, 400x100] '
    showing  '' display=False
  HELD case (D3/D8 must stay closed)
    deferred "Clipboard images aren't read over SSH. Paste a file path instead."
    deferred None   <- retired
