
=== /mcp list
MCP servers
├─ filesystem  disconnected  npx
├─ grafana     connected     https://grafana.example/mcp
├─ linear      connected     https://mcp.linear.app/mcp
└─ notion      disconnected  https://mcp.notion.com/mcp
--- global mcp.json now: ['filesystem', 'grafana', 'linear']

=== /mcp add demo-stdio npx -y demo-mcp
✓ added MCP server 'demo-stdio' to ~/.local-operator/mcp.json
--- global mcp.json now: ['demo-stdio', 'filesystem', 'grafana', 'linear']

=== /mcp add demo-http https://demo.example/mcp
✓ added MCP server 'demo-http' to ~/.local-operator/mcp.json — /mcp login demo-http if it
    needs OAuth
--- global mcp.json now: ['demo-http', 'demo-stdio', 'filesystem', 'grafana', 'linear']

=== /mcp add demo-stdio npx
! could not add MCP server 'demo-stdio': server 'demo-stdio' already exists in
    ~/.local-operator/mcp.json
--- global mcp.json now: ['demo-http', 'demo-stdio', 'filesystem', 'grafana', 'linear']

=== /mcp add demo-http https://demo.example/mcp extra
! /mcp add demo-http <url> takes no extra arguments — got 'extra'
--- global mcp.json now: ['demo-http', 'demo-stdio', 'filesystem', 'grafana', 'linear']

=== /mcp add oops
! usage: /mcp add <name> <url>  |  /mcp add <name> <command> [args…]
--- global mcp.json now: ['demo-http', 'demo-stdio', 'filesystem', 'grafana', 'linear']

=== /mcp remove demo-http
✓ removed MCP server 'demo-http' from ~/.local-operator/mcp.json
--- global mcp.json now: ['demo-stdio', 'filesystem', 'grafana', 'linear']

=== /mcp remove notion
! 'notion' is defined in ~/.claude.json (imported from Claude Code), not by
    local-operator.
Remove it there.
--- global mcp.json now: ['demo-stdio', 'filesystem', 'grafana', 'linear']

=== /mcp remove ghost
! MCP server 'ghost' is not configured — see /mcp
--- global mcp.json now: ['demo-stdio', 'filesystem', 'grafana', 'linear']

=== /mcp frobnicate
! unknown mcp subcommand: frobnicate — try /mcp list|add|remove|login|logout|reauth <name>
--- global mcp.json now: ['demo-stdio', 'filesystem', 'grafana', 'linear']

=== /mcp list
MCP servers
├─ filesystem  disconnected  npx
├─ grafana     connected     https://grafana.example/mcp
├─ linear      connected     https://mcp.linear.app/mcp
├─ demo-stdio  disconnected  npx
└─ notion      disconnected  https://mcp.notion.com/mcp
--- global mcp.json now: ['demo-stdio', 'filesystem', 'grafana', 'linear']

=== final ~/.local-operator/mcp.json
{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "fs-mcp"
      ]
    },
    "grafana": {
      "type": "http",
      "url": "https://grafana.example/mcp"
    },
    "linear": {
      "type": "http",
      "url": "https://mcp.linear.app/mcp",
      "auth": {
        "type": "oauth"
      }
    },
    "demo-stdio": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "demo-mcp"
      ]
    }
  }
}

