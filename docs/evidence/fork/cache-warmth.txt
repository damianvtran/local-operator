=== Cache warmth measurement (design note §4.3 acceptance evidence) ===

Provider: anthropic/claude-opus-5 (live calls, real credentials)
Prefix made unique per run with a nonce, so nothing is warm in advance.

model: anthropic/claude-opus-5
prefix nonce: 4292fc888f5c4305a9108a76beb54a2f
1 parent (cold prefix)       cache_read=0      cache_write=6472   -> WRITE
2 fork (inherited key)       cache_read=6472   cache_write=0      -> READ
3 control (own key)          cache_read=6472   cache_write=0      -> READ

=== Control: does caching work here at all? (same session, 3 calls) ===
model: anthropic/claude-opus-5
same session call 1      input=2 cache_read=5572 cache_write=0
same session call 2      input=2 cache_read=5572 cache_write=0
same session call 3      input=2 cache_read=5572 cache_write=0

=== What this does and does NOT establish ===

ESTABLISHED (Anthropic): a fork's FIRST request reads the parent's warm prefix
in full (6472 of 6472 tokens read, zero written). The clone is byte-identical,
so the prefix is identical, which is what this provider keys on.

NOT ESTABLISHED (OpenAI Responses): the design note's §4.3 acceptance test names
gpt-5.x, and the inherited `prompt_cache_key` only reaches the wire on that path
(`OpenAICompatClient._build_responses_body`). It could not be measured on this
host: the only stored OpenAI credential is a ChatGPT/Codex OAuth account, which
rejects direct model calls --

  ProviderError: invalid request (HTTP 400): The 'gpt-5' model is not supported
  when using Codex with a ChatGPT account.

and the Codex-backed model that IS reachable (gpt-5.6-sol) reported
cache_read=0 / cache_write=0 on every call INCLUDING the same-key control
(same session, identical prefix, twice), so that endpoint reports no cache
counts at all and cannot demonstrate a difference either way.

The control above is what makes the Anthropic number meaningful rather than an
artifact: caching demonstrably works on this account, and the cold-prefix run
writes before the fork reads.

Note the third row: the control session, carrying its OWN key, also reads. That
is the note's §4.2 prediction confirmed -- Anthropic keys the cache on prefix
CONTENT, not on `prompt_cache_key`, so on this provider the inherited key is
neutral. The inheritance is therefore unverified-but-plausible on the one path
where it actually matters, and this evidence should not be read as proving it.
